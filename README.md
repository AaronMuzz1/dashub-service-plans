# Dashub Service Plans

BUILD-001 is the standalone Service Plans product foundation.

Service Plans is independently licensable from Project Blackbook. PB may provide service intelligence, but SP must operate without PB.

## Initial sandbox journey
Quote → Contract → Funding → Claim → PO → Invoice → Weekly dealer payout

Run locally with `npm install` then `npm run dev`.
